{
  "name": "simple-node-server",
  "version": "1.0.0",
  "description": "A simple HTTP server in nodeJS",
  "main": "server.ts",
  "scripts": {
    "start": "ts-node server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Umer",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.5.1",
    "typescript": "^5.5.4"
  }
}
